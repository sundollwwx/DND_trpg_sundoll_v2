# M4 结果报告：项目棋子库与空间交互第一批

日期：2026-08-25
状态：**M4 第一批已实现，整体 M4 仍为 Partial**

## 本批已完成

- 新增可扩展的 `Asset / Definition / Instance` 纯数据模型。
- 资产使用现有 M2 内容寻址存储，重复二进制按 SHA-256 去重。
- 图片或缩略图文件缺失时保留资产记录和棋子实体，不静默删除。
- 新增 `Unplaced`、`OnBoard`、`InContainer`、`Attached` 四种互斥位置。
- 支持棋盘堆叠顺序、收入容器、附着、解除关系、旋转、翻面和显隐。
- 容器/附着关系提交前进行循环检测；越界位置、未知定义和未知目标会拒绝。
- M4 命令接入现有 Command Bus、Undo/Redo、版本化 Command Envelope 和 Journal 重放。
- Workbench 使用新的本地开发根目录 `SundollWorld_M4`，不改写既有 M1/M2/M3 开发样本。
- Workbench 增加“新增占位棋子”入口，缺少正式图片时使用可替换的占位色块显示。

## 验证证据

- EditMode：65/65 通过，包含 M4 8 项纯数据、关系、Hash、Journal 和资产去重测试。
- PlayMode：2/2 通过，包含 Workbench 启动/地图投影和 M4 占位棋子投影/销毁重建。
- Unity：`6000.3.22f1`；本轮没有新增 C# 编译错误。

证据文件：

- [EditMode 65/65](../TestResults_EditMode_20260825_m4_full2.xml)
- [PlayMode 2/2](../TestResults_PlayMode_20260825_m4_full2.xml)

## 尚未关闭

- 正式棋子库面板：分类、标签编辑、搜索、筛选和多条目管理 UI。
- 运行时图片选择/导入和 Unity 纹理缩略图生成；当前已完成存储底座和占位显示。
- 棋子在 Workbench 中的拾取、多选、移动、堆叠顺序调整、收纳和附着操作 UI。
- 纹理生命周期预算、棋子数量基准和性能 p95。
- M5 主控台中的多地图主持工作流。

这批代码仍属于 pre-v1 数据格式；M7 之前不承诺开发存档长期兼容。
