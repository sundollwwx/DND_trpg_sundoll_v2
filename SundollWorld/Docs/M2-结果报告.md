# M2 结果报告

日期：2026-08-24  
范围：新格式存档与内容基础最小切片  
状态：M2 核心实现与 macOS 验证完成；Windows、跨平台互开和完整强制退出矩阵仍待补做，因此不宣称 M2 全部退出条件已完成。

## 当前实现

- 正式工程：`/Users/sundoll/Desktop/SundollUnity/SundollWorld`
- Unity：`6000.3.22f1`，Universal 2D / URP 2D Renderer。
- 新增 M2 基础设施：`M2PersistenceModels`、`M2FileIO`、`M2CanonicalStateHasher`、`M2ProjectStore`、`M2JournalStore`、`M2SaveSession`、`M2ContentBlobStore`、`M2PackageArchive`。
- 运行时面板已接入：验证存档、手动保存 Snapshot、重新加载、导出 `.sundollpkg`，并显示当前 HEAD 与待自动保存事务数。

## M2 功能覆盖

1. `HEAD.json`、不可变 Revision 目录和 Revision Manifest；HEAD 使用 generation、active revision 与 last-known-good revision。
2. UTF-8 原子写入、临时文件、`Flush(true)`、`File.Replace`/首次 `Move`，并在提交 HEAD 前后保留故障注入点。
3. Canonical SHA-256：对状态字段使用稳定顺序编码，地图单元格按坐标和内容 ID 排序，JSON 往返不会改变状态 Hash。
4. 分段 Journal：每条记录带 payload 校验和与状态 Hash；损坏尾部会被跳过，只恢复最近一条完整记录。
5. 自动保存策略：默认每 25 个正式事务或 180 秒触发；项目存储默认保留最近 10 个 Revision，并始终保护当前 HEAD/LKG。
6. 内容寻址 blob：用户资产与缩略图按 SHA-256 去重、读回时校验长度和 Hash，单 blob 上限为 64 MiB。
7. `.sundollpkg`：导出已验证的活动 Revision、HEAD 和内容 blob；导入使用 staging 目录并在完整性验证后原子移动。
8. 包安全检查：限制 Entry 数量 4096、解压总大小 256 MiB，拒绝路径穿越、绝对路径、反斜杠、重复 Entry 和未知顶层路径。

存档格式当前为 format/world schema `1` 的 pre-v1 草案，不承诺长期兼容，也没有读取历史网页项目格式的路径。

## 自动化验证

- EditMode：共 12 个测试，12 通过、0 失败、0 忽略；其中 M1 既有测试 4 个，M2 Persistence 测试 8 个。
- M2 测试覆盖：Canonical Hash、不可变 Revision/有效 HEAD、HEAD 提交故障注入、Journal 分段与损坏尾部恢复、blob Hash 校验、`.sundollpkg` 往返、路径穿越拒绝、事务阈值自动保存。
- Unity Editor：脚本重新导入和编译完成；清理后的 Console 为 0 error / 0 warning。所有新增 C#、asmdef、场景与测试文件均有对应 `.meta` 文件。
- Play Mode：已实际验证 HEAD/Revision 初始化、存档验证、移动写入 Journal、手动保存新 Revision、导出 `.sundollpkg`，以及移动后重新加载恢复。
- macOS 构建：Build 成功，产物为 `Builds/M2-macOS.app`；主可执行文件为 x86_64 + arm64 universal binary，约 115 MiB，并已启动独立构建查看 M2 运行时面板。

## 实际产物与存档样本

- 构建：`/Users/sundoll/Desktop/SundollUnity/Builds/M2-macOS.app`
- 存档目录：`/Users/sundoll/Library/Application Support/DefaultCompany/SundollWorld/SundollWorld/SundollWorld`
- 便携包：`/Users/sundoll/Library/Application Support/DefaultCompany/SundollWorld/SundollWorld/SundollWorld-M2.sundollpkg`
- 当前样本 HEAD generation：2；包含 2 个 Revision 和 Journal segment。

## 限制与下一步

- 未执行 Windows Build、Windows 原子写盘、Windows/macOS 跨平台互开，以及两平台上的强制退出恢复矩阵。
- 未做磁盘满、无权限、并发保存、真实设备、性能预算或长时间编辑测试。
- 本轮未覆盖“HEAD.json 自身损坏后自动扫描 Revision”的恢复路径；当前实现覆盖的是 HEAD 提交失败后的旧 HEAD 保留、LKG/最佳 Revision 选择和 Journal 尾部损坏恢复。
- Bootstrap 场景仍没有 Camera，Unity Game 视图会显示 `No cameras rendering`；M2 面板是诊断用 IMGUI，不是最终工作台 UI。
- 独立构建的按钮自动化句柄不稳定，因此移动、保存和重载的交互证据来自 Unity Editor Play Mode。
- 工程及工作区上级目录未检测到 Git repository，本轮没有可记录的 commit 或 working-tree diff。

M2 后续应先补齐 Windows 与跨平台存档验证，再进入 M3 地图制作器 MVP；在此之前不应把当前 pre-v1 存档格式作为公开兼容承诺。
