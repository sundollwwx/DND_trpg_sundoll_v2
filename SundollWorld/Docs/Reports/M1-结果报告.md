# M1 结果报告

日期：2026-08-24  
状态：M1 已完成验证；核心闭环通过，状态为 Ready with limitations。

## 当前实现

- 正式工程：`/Users/sundoll/Desktop/SundollUnity/SundollWorld`
- Unity：`6000.3.22f1`，Universal 2D / URP 2D Renderer。
- 首批程序集：`Sundoll.Core`、`Sundoll.Application`、`Sundoll.Infrastructure`、`Sundoll.Presentation`、`Sundoll.Bootstrap`、`Sundoll.Editor`、`Sundoll.Tests.EditMode`。
- 领域链路：

  `MapDocument → PublishMapContent → MapContentVersion → ScenarioDocument → BoardInstance → PieceInstance.OnBoard`

- Application：`AllowAllRulePolicy`、`M1LocalAuthority`、命令幂等/Revision 冲突、`M1CommandBus`、Undo/Redo。
- Infrastructure：UTF-8 JSON Snapshot、临时文件、`Flush(true)` 和替换写入。
- Presentation：运行时 M1 状态面板，可移动棋子、Undo、Redo、保存和重新加载。
- Bootstrap：无场景依赖的运行时启动兜底，以及可通过 `Sundoll/M1/Create Bootstrap Scene` 创建正式启动场景的 Editor 菜单。
- EditMode：纵向链路、Undo/Redo、LocalAuthority 幂等/冲突、纯数据 Snapshot 重建测试。

## 已完成静态验证

- 7 个 asmdef 文件可解析，引用方向符合 Core → Application → Infrastructure/Presentation → Bootstrap。
- Core 与 Application 未引用 `UnityEngine` 或 `UnityEditor`。
- 正式工程的 `Assets/Sundoll/` 已建立，未复用 M0 Spike 的 Assets。

## M1 验证结果

1. Unity Editor：6000.3.22f1 导入并编译完成；修复后的初始 Console 为 0 error / 0 warning。
2. EditMode：`Sundoll.Tests.EditMode` 共 4 个测试，4 通过、0 失败、0 忽略。
3. Bootstrap 场景：已通过 `Sundoll/M1/Create Bootstrap Scene` 创建；Build Settings 仅包含 `Assets/Sundoll/Scenes/M1Bootstrap.unity`。
4. Unity Play Mode：运行时面板显示完整领域链路；已实际验证向右移动、Undo、Redo、保存 Snapshot、移动后重新加载 Snapshot。
5. Snapshot：已在 `Application.persistentDataPath` 生成有效 UTF-8 JSON；验证了保存后重新加载可恢复状态。
6. macOS Smoke Build：Build Profiles 使用 macOS、Intel 64-bit + Apple silicon，Build 结果为 `Succeeded`（约 51 秒）。产物为 `Builds/M1-macOS.app`，主可执行文件为 x86_64 + arm64 universal binary；独立构建已成功启动并显示 M1 运行时面板。

## Console 与构建说明

- 最终 Console：0 error；构建结束后有 1 条 warning，来自 Unity Cloud Services 上传 native symbols 时的网络 `SecureChannelFailure`。该 warning 不影响本地 Build 成功，归类为环境相关，不是 M1 代码编译错误。
- 构建产物位于工程目录外的 `../Builds/M1-macOS.app`，未写入 `Assets/`。
- 工程目录及其工作区上级目录未检测到 Git repository，因此本次没有 commit/working-tree diff 可记录。

## 限制与未验证项

- Bootstrap 场景当前没有 Camera；Unity Editor Game 视图会显示 `No cameras rendering`，但 IMGUI 状态面板和独立 macOS 构建均可显示。当前 M1 仍是诊断面板，不是最终棋盘/棋子视觉呈现。
- 未执行 Windows 构建与跨平台互开；未做真实设备覆盖、性能预算、网络/多人验证。
- 独立 macOS 构建已启动验证，但其按钮自动化窗口句柄不可用，因此移动、Undo/Redo、保存/重载交互证据来自 Unity Editor Play Mode。
- M1 仍是 pre-v1 单项目/单地图/单棋子纵向 Spike，不包含正式内容编辑器、用户图片 blob、崩溃 Journal、便携包或网络。
