# M6 结果报告：规则接缝与 Loopback 证明

日期：2026-08-25
状态：**M6A/M6B 进程内证明完成；真实网络未纳入本阶段**

## M6A

- 新增独立 `M6RulePipeline`，支持 `Allow / Deny / Replace / Append`。
- 规则接缝不修改 Core 命令类型、M2 Save 外壳和主 Workbench UI。
- `M6RuleSeamTests` 覆盖四种决策和诊断链路。

## M6B

- 新增传输无关的 `M6ProjectionSnapshot` 和 `M6ProjectionDelta` DTO。
- `M6LoopbackHub` 作为唯一权威端；客户端只消费 Snapshot 和 versioned Command Envelope。
- 覆盖客户端幂等边界、revision 冲突、断线后的 Delta Tail 追赶、Projection Hash 收敛和 Audience Projection（迷雾/棋子过滤基础）。
- 未添加真实网络框架、账号、房间或 Relay。

## 验证证据

- EditMode：`TestResults_EditMode_20260825_m5m6m7.xml`，76/76 通过，其中包含 M6A 1 项、M6B 2 项。
- PlayMode：`TestResults_PlayMode_20260825_m5m6m7.xml`，4/4 通过。

## 未关闭

- 两个真实独立进程、真实网络抖动、丢包/重传和跨平台互开仍未验证。
- M6B 当前是 Loopback architecture proof，不是联网版本承诺。
