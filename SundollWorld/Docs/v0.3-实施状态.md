# SundollWorld v0.3 实施状态

日期：2026-08-25
用途：记录当前可复核基线、验证证据与未关闭限制；本文件不替代各里程碑结果报告。

## 版本控制基线

- 工作区根目录已经初始化 Git，分支为 `main`。
- 仓库本地身份为 `Sundoll <sundoll@localhost>`。
- 首个实现基线提交为 `b8d704f`（`chore: capture SundollWorld v0.3 implementation baseline`）。
- `.gitignore` 排除 Unity 缓存、临时文件、日志、用户设置、构建产物、IDE 文件和 `.DS_Store`。
- 现有约 11 GB Unity 缓存与构建产物保持原位，没有删除，也没有纳入版本控制。

## 当前验证证据

- 2026-08-25 使用 Unity `6000.3.22f1` 生成新的完整 EditMode XML：45/45 通过、0 失败、0 忽略，耗时约 1.18 秒。
- 本次完整结果包含 M1 4/4、M2 19/19 和 M3 22/22；M2 新增覆盖 Journal v2 命令 Payload 重放、v1/v2 混合重放、Snapshot 后未保存命令恢复和损坏尾部恢复。
- M3 定向 XML 为 22/22 通过，覆盖版本化 `WorldChangeSet` JSON 往返、运行时索引、正向/逆向 Delta 和局部 Undo/Redo 脏区。
- 首次独立复跑曾因沙箱 Package Manager socket 权限与遗留 Licensing Client 协议冲突而阻塞；允许必要系统访问并重启失效的版本专用 Licensing Client 后已经恢复。
- 最新完整结果已作为仓库证据保存：[TestResults_EditMode_20260825_104204.xml](../TestResults_EditMode_20260825_104204.xml)。此前的 `/tmp/SundollWorld-v03-envelope-full.xml`、`/tmp/SundollWorld-CommandEnvelope.xml` 和 `/tmp/SundollWorld-M3Delta.xml` 仍是历史本机执行证据。

## 里程碑状态

| 阶段 | 状态 | 已确认 | 仍需关闭 |
| --- | --- | --- | --- |
| M0 | Accepted with limitations | 数据验证、Unity 验证、macOS IL2CPP、Windows Mono 构建 | Windows IL2CPP/写盘、Finder 真实拖放 |
| M1 | Ready with limitations | 纵向闭环、纯数据重建、4/4 测试、macOS universal build | Windows Smoke；正式构建仍为 Mono |
| M2 | Partial | Revision、HEAD/Revision 扫描恢复、generation 冲突、Journal、blob、便携包、CommandEnvelope/AcceptedOperationBatch、Journal v2 Payload 重放；M2 19/19 | 异步保存、真实无权限/磁盘满、Windows 矩阵 |
| M3 | Partial | 批量绘制、几何工具、五图层、运行时索引、Delta Undo、Dirty Region、缓存，最新全量证据 45/45 | Workbench、选择/复制旋转、门箱对象、性能门槛 |

## 当前执行顺序

1. 已完成 Journal 状态缓存、损坏尾部续写、HEAD 缺失/损坏扫描、generation 冲突和旧 HEAD 保留自动化。
2. 已完成地图运行时索引、版本化 `WorldChangeSet`、CommandEnvelope、AcceptedOperationBatch 和 Delta Undo/Redo。
3. 已接入 Journal 命令 Payload 重放，并保持旧开发存档读取路径。
4. 建立单写入异步保存队列和明确的保存状态。
5. 用正交 Camera、Tilemap 与 UI Toolkit 建立正式 Workbench，再补齐选择、剪贴板、旋转、图层排序及门/箱对象。

Windows IL2CPP、Windows 原子写盘、跨平台互开和双平台强制退出矩阵是发布阻塞项，最迟必须在 M7 Beta 候选前通过。
