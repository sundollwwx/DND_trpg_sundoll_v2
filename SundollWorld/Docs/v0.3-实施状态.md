# SundollWorld v0.3 实施状态

日期：2026-08-25
用途：记录当前可复核基线、验证证据与未关闭限制；本文件不替代各里程碑结果报告。

## 版本控制基线

- 工作区根目录已经初始化 Git，分支为 `main`。
- 仓库本地身份为 `Sundoll <sundoll@localhost>`。
- 首个实现基线提交为 `b8d704f`（`chore: capture SundollWorld v0.3 implementation baseline`）。
- `.gitignore` 排除 Unity 缓存、临时文件、日志、用户设置、构建产物、IDE 文件和 `.DS_Store`。
- 现有约 11 GB Unity 缓存与构建产物保持原位，没有删除，也没有纳入版本控制。

## 当前验证证据

- 最近一次完整 M3 EditMode 结果为 33/33 通过，Console 为 0 error、0 warning；详情见 [M3 结果报告](./M3-结果报告.md)。
- 2026-08-25 关闭占用工程的旧 Editor 后，尝试使用 Unity `6000.3.22f1` 独立批处理复跑。
- 第一次运行因沙箱无权创建 Unity Package Manager socket 而未进入测试。
- 允许所需系统访问后的第二次运行被 Unity Licensing Client 握手问题阻塞，日志包含 `Unsupported protocol version '1.18.1'` 与 Licensing Client `ObjectDisposedException`。
- 该批处理没有生成测试 XML，因此只能记为环境阻塞，不能宣称为新的测试通过或项目测试失败。

## 里程碑状态

| 阶段 | 状态 | 已确认 | 仍需关闭 |
| --- | --- | --- | --- |
| M0 | Accepted with limitations | 数据验证、Unity 验证、macOS IL2CPP、Windows Mono 构建 | Windows IL2CPP/写盘、Finder 真实拖放 |
| M1 | Ready with limitations | 纵向闭环、纯数据重建、4/4 测试、macOS universal build | Windows Smoke；正式构建仍为 Mono |
| M2 | Partial | Revision、HEAD、Journal、blob、便携包、8 项 M2 测试 | HEAD 恢复、并发/磁盘错误、异步保存、Windows 矩阵 |
| M3 | Partial | 批量绘制、几何工具、五图层、Dirty Region、缓存，最近证据 33/33 | Workbench、选择/复制旋转、门箱对象、性能门槛 |

## 当前执行顺序

1. 在不破坏旧开发存档的前提下稳定 M2/M3：Journal 缓存、HEAD 恢复、故障注入、Delta 与版本化命令。
2. 建立单写入异步保存队列和明确的保存状态。
3. 用正交 Camera、Tilemap 与 UI Toolkit 建立正式 Workbench，旧 IMGUI 仅保留为诊断入口。
4. 补齐选择、剪贴板、旋转、图层排序及门/箱对象。
5. 关闭 macOS EditMode、PlayMode、端到端、性能和 universal IL2CPP 构建门槛。

Windows IL2CPP、Windows 原子写盘、跨平台互开和双平台强制退出矩阵是发布阻塞项，最迟必须在 M7 Beta 候选前通过。
