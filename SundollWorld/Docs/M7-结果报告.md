# M7 结果报告：v1 加固与发布边界

日期：2026-08-25
状态：**macOS 可验证范围已完成第一批；Beta 发布矩阵仍 Partial/Blocked**

## 本批完成

- 冻结常量：Save Format v1、World Schema 2、Migration Registry v1、Unity `6000.3.22f1`。
- 建立 Schema 1→2 迁移注册表，旧列表/对象字段缺失时补默认值，不改写旧文件。
- 增加 Frozen Save 文档、Canonical Hash 校验和 Golden Save/tamper 测试。
- 增加可复用对象池和引用计数纹理缓存；缺失或哈希错误资源回退占位，不删除棋子实体。
- 增加百分位性能采样工具，作为后续正式门槛的统一测量入口。

## 验证证据

- 最新 EditMode：`TestResults_EditMode_20260825_m5_hierarchy_context.xml`，78/78 通过。
- 最新 PlayMode：`TestResults_PlayMode_20260825_m5_hierarchy_context.xml`，6/6 通过，包含 Workbench Hierarchy/右键菜单启动检查、纹理缓存、64 个棋子纹理投影基线和 M5 投影生命周期测试。
- 测试入口通过 `M7BuildValidation` 的 Unity TestRunnerApi 程序化运行，保留 XML 证据；命令行 runner 在本机因缺少 .NET SDK 的 `build-server` 启动器未产出 XML。
- Unity 编译：`6000.3.22f1`，本轮新增代码 0 编译错误、EditMode/PlayMode 均 0 测试失败。
- macOS 性能基线（Unity Editor/本机临时目录，5 次样本；Journal 恢复 3 次）：256×256 批量编辑 p95 `33.588 ms`、Snapshot 捕获 p95 `2.253 ms`、Revision 保存 p95 `360.869 ms`、10,000 Journal Batch 恢复 p95 `722.057 ms`。四项分别低于 `<50 ms`、`<16 ms`、`<2 s`、`<5 s` 门槛；这不是 2560×1440 真实窗口帧率或分配预算证明。
- macOS universal IL2CPP：`Build Finished, Result: Success`；最新 M5 投影版产物为 `Builds/SundollWorld-v03-M7-macOS-universal.app`，约 148 MiB，主可执行文件为 `x86_64 + arm64` universal。Unity `BuildReport.totalErrors=7838`，逐条均为 `TypeDB: Class ... was already registered` 的编辑器重复类型诊断；没有 `error CS`/脚本编译错误，但该环境诊断仍保留为发布 caveat。
- 独立 Player Smoke：最新产物无图形启动成功，日志确认初始化 `6000.3.22f1`、Input System 和 Null GfxDevice；本 Smoke 未执行 Windows 或跨平台场景。
- M4 原生选择器：macOS Editor 路径已接入系统 Open 面板，PlayMode 已确认 `PickPieceImageFile` 控件存在；真实文件选择/取消交互仍需在有窗口焦点的 macOS Editor/Player 手动复验。
- M4 纹理基线：64 个棋子实例共享 1 张运行时纹理，重复投影刷新 p95 `0.121 ms`、最大 `0.121 ms`；该结果是 macOS Unity Editor 基线，不等同于 2560×1440 真窗口帧率证明。

## 发布阻塞项

- macOS universal IL2CPP 本轮已重建并启动 Smoke；正式视觉操作和长时间运行仍未测，TypeDB 重复类型诊断也需要在 Beta 前进一步清理或由 Unity 版本/包环境确认。
- 2560×1440 真实窗口稳定 60 FPS、稳态分配 p95 不超过 1 KB/帧、长时间编辑和正式场景纹理数量门槛尚未验证；64 个棋子纹理共享与刷新基线已形成，批量编辑、Snapshot、Revision 保存和 10,000 Journal 恢复已形成 macOS 基线并通过对应数值门槛。
- Windows IL2CPP、Windows 原子写盘、Windows/macOS 跨平台互开、双平台强制退出恢复均因当前没有 Windows 环境保持 Blocked。
- Finder 真实拖放仍未实现；原生文件选择器已接入，但独立窗口选择/取消 Smoke 尚未记录为正式证据。
