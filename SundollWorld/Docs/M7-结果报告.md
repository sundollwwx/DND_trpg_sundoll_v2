# M7 结果报告：v1 加固与发布边界

日期：2026-08-27
状态：**macOS 可验证范围已完成第一批；Beta 发布矩阵仍 Partial/Blocked**

## 本批完成

- 冻结常量：Save Format v1、World Schema 2、Migration Registry v1、Unity `6000.3.22f1`。
- 建立 Schema 1→2 迁移注册表，旧列表/对象字段缺失时补默认值，不改写旧文件。
- 增加 Frozen Save 文档、Canonical Hash 校验和 Golden Save/tamper 测试。
- 增加可复用对象池和引用计数纹理缓存；缺失或哈希错误资源回退占位，不删除棋子实体。
- 增加百分位性能采样工具，作为后续正式门槛的统一测量入口。
- 增加棋子库虚拟化缩略图网格，定义列表不再用长按钮全量堆叠；实例列表独立显示，导入按钮改为“导入并重新绑定当前定义”，重复图片会复用现有内容。
- 增加 `scripts/unity-doctor.sh` 与 `scripts/unity-run-tests.sh`，固定 Unity `6000.3.22f1` 路径进行连接自检和 batchmode 验证。
- 记录 Unity 连接诊断：当前没有 Unity MCP bridge/relay；近期反复“找不到连接”的主要证据是 Unity Licensing Client 通道协议不匹配、客户端崩溃/超时和网络证书/远程配置噪声。

## 验证证据

- 最新 EditMode：`TestResults_EditMode_20260827_150046.xml`，83/83 通过。
- 最新 PlayMode：`TestResults_PlayMode_20260827_150000.xml`，11/11 通过，包含 Workbench Hierarchy/右键菜单启动检查、纹理缓存、64 个棋子纹理投影基线、M5 投影生命周期测试、棋子库虚拟化网格过滤和缩略图 LRU/缺失代理测试。
- 测试入口通过 `M7BuildValidation` 的 Unity TestRunnerApi 程序化运行，保留 XML 证据；本轮在无 Player 干扰的 macOS 批处理环境中重新产出 EditMode/PlayMode XML。
- Unity 编译：`6000.3.22f1`，本轮新增代码 0 编译错误、EditMode/PlayMode 均 0 测试失败。
- macOS 性能基线（Unity 批处理/本机临时目录，5 次样本；Journal 恢复 3 次）：2026-08-27 复跑结果为 256×256 批量编辑 p95 `21.686 ms`、Snapshot 捕获 p95 `2.355 ms`、Revision 保存 p95 `451.582 ms`、10,000 Journal Batch 恢复 p95 `619.959 ms`。四项分别低于 `<50 ms`、`<16 ms`、`<2 s`、`<5 s` 门槛；这不是 2560×1440 真实窗口帧率或分配预算证明。
- macOS universal IL2CPP：最新一轮 `Build Finished, Result: Success`；产物为 `Builds/SundollWorld-v03-M7-macOS-universal.app`，BuildReport 字节数 `121,065,864`（约 115.5 MiB），主可执行文件确认是 `x86_64 + arm64` universal。主可执行文件 SHA-256：`46ec3f97adbc1de6147ea30a25d6d57b505547f03fb260ded7b6c5412f0565e1`。Unity `BuildReport.totalErrors=11798`，逐条均为 `TypeDB: Class ... was already registered` 的编辑器重复类型诊断；没有 `error CS`/脚本编译错误，但该环境诊断仍保留为发布 caveat。
- 独立 Player Smoke：本轮最新产物实际启动并显示 Workbench UI/地图视口与左右栏滚动条；Player.log 确认 Unity `6000.3.22f1`、Input System、Metal Apple M5 和窗口 surface `3420×2146`，未发现 `error CS`、`NullReferenceException`、`MissingReferenceException` 或 `ArgumentException`，随后正常退出。本 Smoke 未执行 Windows 或跨平台场景。
- M4 原生选择器：最新 Player 中实际打开系统 Open 面板并选择临时 PNG，返回 Workbench 正常；随后再次打开并用 Escape 取消，面板关闭并回到 Workbench，状态回退可读，未点击导入，因此没有改写项目数据。
- M4 纹理基线：64 个棋子实例共享 1 张运行时纹理，重复投影刷新 p95 `0.121 ms`、最大 `0.121 ms`；该结果是 macOS Unity Editor 基线，不等同于 2560×1440 真窗口帧率证明。
- M7 棋子库网格截图：`Docs/Evidence/M7-piece-library-grid-20260826.jpg` 和 `Docs/Evidence/M7-piece-library-selection-20260826.jpg`，记录双列缩略图、选中态、缺失提示和独立实例区。

## 发布阻塞项

- macOS universal IL2CPP 本轮已重建并启动 Smoke；正式视觉操作和长时间运行仍未测，TypeDB 重复类型诊断也需要在 Beta 前进一步清理或由 Unity 版本/包环境确认。
- 2560×1440 真实窗口稳定 60 FPS、稳态分配 p95 不超过 1 KB/帧、长时间编辑和正式场景纹理数量门槛尚未验证；64 个棋子纹理共享与刷新基线已形成，批量编辑、Snapshot、Revision 保存和 10,000 Journal 恢复已形成 macOS 基线并通过对应数值门槛。
- Windows IL2CPP、Windows 原子写盘、Windows/macOS 跨平台互开、双平台强制退出恢复均因当前没有 Windows 环境保持 Blocked。
- Finder 真实拖放仍未实现；原生文件选择器选择/取消路径已通过 Player Smoke，但正式图片管理视觉体验、2560×1440 帧率/分配预算和长时间运行仍未关闭。
- 没有 Unity MCP bridge 时，Codex 无法直接读取实时 Editor Console/Hierarchy；如需这类能力，需要后续单独选择并安装一个 Unity MCP provider。
