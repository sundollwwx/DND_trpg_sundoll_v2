# SundollWorld 正式工程文档

- `Reports/`：M1–M7 各阶段完成内容、验证结果与未关闭风险。
- `Planning/`：当前 v0.3 实施状态。
- `Guides/`：Unity/许可证连接诊断与 Windows 验证交接。
- `Evidence/`：已提交的构建、性能、截图与 XML 测试证据。
- `AI/`：工程架构与协作上下文。

Unity 每次新运行产生的原始测试 XML 会进入 `../../TestResults/Local/`，该目录不提交到 Git；需要长期引用的结果再筛选至 `Evidence/TestResults/`。
