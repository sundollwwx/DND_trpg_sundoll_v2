# SundollWorld v0.3 实施状态

日期：2026-08-31
用途：记录当前可复核基线、验证证据与未关闭限制；本文件不替代各里程碑结果报告。

## 版本控制基线

- 工作区根目录已经初始化 Git，分支为 `main`。
- 仓库本地身份为 `Sundoll <sundoll@localhost>`。
- 首个实现基线提交为 `b8d704f`（`chore: capture SundollWorld v0.3 implementation baseline`）。
- `.gitignore` 排除 Unity 缓存、临时文件、日志、用户设置、构建产物、IDE 文件和 `.DS_Store`。
- 现有约 11 GB Unity 缓存与构建产物保持原位，没有删除，也没有纳入版本控制。

## 当前验证证据

- 2026-08-25 使用 Unity `6000.3.22f1` 生成新的 M5-M7 EditMode XML：78/78 通过、0 失败、0 忽略；本轮接入 M4 原生选择器和 64 棋子纹理基线并重新生成证据，PlayMode 为 6/6。
- 2026-08-27 使用固定 Unity batchmode 脚本复跑：EditMode 84/84 通过，PlayMode 12/12 通过；随后新增 1000 棋子投影性能测试。2026-08-29 通过固定版本专用许可证通道再验证 EditMode 87/87、PlayMode 13/13，结果文件为 `TestResults_EditMode_20260829_212538.xml` 和 `TestResults_PlayMode_20260829_212538.xml`。
- 完整 EditMode 最新为 87/87，包含 M1、M2、M3、M4、M5、M6、M7、项目工作区和内置素材覆盖；新增覆盖保存队列串行化、Canonical Hash 空对象兼容、剪贴板越界原子拒绝、顶层可见层拾取、门箱稳定 ID/动作/发布深拷贝、Schema 1→2 默认字段、Workspace State 2/3、项目中心、内置素材和性能基线。
- M3 PlayMode 覆盖 `M3Workbench.unity` 启动、Tilemap 投影、Workbench 编辑后脏区刷新、隐藏/锁定和 View 销毁清理。
- 首次独立复跑曾因沙箱 Package Manager socket 权限与遗留 Licensing Client 协议冲突而阻塞；允许必要系统访问并重启失效的版本专用 Licensing Client 后已经恢复。
- 2026-08-27 新增 Unity 连接诊断记录与固定 batchmode 脚本；2026-08-28 将许可证通道、孤立客户端清理、锁文件保护、健康检查和 macOS 构建统一到 `scripts/unity-common.sh`；当前未检测到 Unity MCP bridge 或官方 relay，实时 Editor MCP 工具不可用，验证默认走精确 Unity `6000.3.22f1` 路径和 XML/日志证据。
- M7 稳定化 batchmode 证据为：[EditMode XML](../Evidence/TestResults/TestResults_EditMode_20260830_133200.xml) `87/87` 和 [PlayMode XML](../Evidence/TestResults/TestResults_PlayMode_20260830_133200.xml) `13/13`，均 0 失败、0 忽略；两次日志均确认版本专用许可证通道，独立探针也通过。新增 1000 可见棋子投影刷新 p95 `12.494 ms`、稳态分配 p95 `0 B`；真实 Player 渲染承载 p95 `4.5495 ms`、分配 p95 `0 B`。旧 M1/M2/M3 开发存档保持不动；Workbench M4/M5 使用新的 `Application.persistentDataPath/SundollWorld_M4`。
- 2026-08-30 完成 Workbench 棋子交互批次，并使用同一固定通道验证 EditMode `92/92`、PlayMode `14/14`。本机 XML 为 `TestResults_EditMode_20260830_212353.xml` 与 `TestResults_PlayMode_20260830_212353.xml`；覆盖批量移动/显示/删除、Journal 重放、无稳定 ID 棋盘保护，以及多选拖动 Ghost 的完整投影流程。
- 2026-08-30 完成 M5 多地图工作区恢复：Map ID 对应独立、可移植的工作区状态文件，旧单文件状态可继续读取；切图前异步保存草稿，切图后恢复地图专属视口、图层、工具和素材选择。固定许可证通道验证 EditMode `93/93`、PlayMode `15/15`，0 失败、0 跳过，结果为 `TestResults_EditMode_20260830_214327.xml` 与 `TestResults_PlayMode_20260830_214327.xml`。
- 未使用的 Visual Scripting 包已从 `manifest.json` 与 `packages-lock.json` 移除；干净临时构建中 TypeDB 重复类型诊断为 0，剩余 1 条 Build warning 来自 Unity Cloud symbols 上传 token 为空。
- 2026-08-31 完成 UX4 棋子库页面收尾：名称/标签搜索、动态分类筛选、缩略图状态筛选、组合筛选、虚拟化网格、筛选结果/缓存计数，以及当前定义编辑和素材导入分区；列表刷新缓存同时考虑筛选状态。固定许可证通道复跑 EditMode `93/93`、PlayMode `16/16`，0 失败、0 跳过，证据见 `Docs/Evidence/M7-piece-library-ux4-20260831.md`。
- 2026-08-31 完成 UX5 主控台第一批：地图管理、当前主持状态、迷雾笔刷、动态标注和交互对象改为独立视觉区块；主控台/层级标题和操作提示统一；移除 Unity 6 不支持的 `line-height` USS 声明。固定许可证通道复跑 EditMode `93/93`、PlayMode `16/16`，0 失败、0 跳过，未发现编译错误或 USS 无效属性警告；证据见 `Docs/Evidence/M7-workbench-ux5-20260831.md`。日志中的 `Access token is unavailable` 属于可选远程刷新提示，本地 entitlement 已成功解析。

## 里程碑状态

| 阶段 | 状态 | 已确认 | 仍需关闭 |
| --- | --- | --- | --- |
| M0 | Accepted with limitations | 数据验证、Unity 验证、macOS IL2CPP、Windows Mono 构建 | Windows IL2CPP/写盘、Finder 真实拖放 |
| M1 | Ready with limitations | 纵向闭环、纯数据重建、4/4 测试、macOS universal build | Windows Smoke；正式构建仍为 Mono |
| M2 | Partial | Revision、HEAD/Revision 扫描恢复、generation 冲突、Journal、blob、便携包、CommandEnvelope/AcceptedOperationBatch、Journal v2 Payload 重放、单写入后台保存队列、保存状态、OS 文件写锁、锁超时和故障注入；M2 26/26 | 真实磁盘满/权限、独立进程强制退出、Windows/跨平台矩阵 |
| M3 | Ready with limitations | 正式 Workbench、三工作区、Tilemap 五层投影、Input System 编辑/视口、选择/复制/剪切/粘贴/旋转、Workspace State 2、门箱对象、Schema 2；UX1–UX5 外壳、边界/网格/工具栏/素材卡片、情境 Inspector、主控台情境分区；最新 EditMode 93/93、PlayMode 16/16、macOS universal IL2CPP + Smoke | UX6–UX7 视觉收尾、长时 60 FPS pacing、真实鼠标复验；Windows/跨平台矩阵 |
| M4 | Ready with limitations | Asset/Definition/Instance、内容寻址去重、四种位置关系、堆叠顺序/收纳/附着/解除、旋转/翻面/显隐、关系校验、Command/Journal 接入、棋子库搜索/分类与缩略图状态筛选/实例列表/路径导入/原生选择器入口/关系操作 UI、点击/框选/多选/拖动 Ghost、批量移动/显示/删除、稳定棋盘创建与修复、占位与真实纹理投影、引用计数缓存；UX4 视觉分区与组合筛选；EditMode 93/93、PlayMode 16/16 | 素材导入完整鼠标流程、正式场景纹理/长时间性能门槛；Windows/跨平台矩阵 |
| M5 | Ready with limitations | 多地图槽位创建/切换/重命名、每地图独立工作区/视口恢复与旧状态兼容、切图前异步保存、迷雾格、迷雾揭示/隐藏笔刷、动态标注创建/拖动/编辑、交互状态、命令/Undo/Journal/Hash、Workbench 创建/切换与主持入口、遮罩/标注投影、Hierarchy、右键通用交互菜单、基于 M6 Audience Projection 的玩家预览只读入口、UX5 主控台情境分区；EditMode 93/93、PlayMode 16/16 | 真实窗口操作手感、批量主持长时间性能、多地图玩家视图实机复验 |
| M6A | Ready with limitations | 独立规则接缝 Allow/Deny/Replace/Append 和测试 | 尚未接入具体玩法规则 |
| M6B | Ready with limitations | Loopback Hub、Projection Snapshot/Delta Tail、revision 冲突、断线追赶、Audience Projection、Hash 收敛 | 真实网络、独立进程、跨平台互开 |
| M7 | Partial / Blocked | Save v1/schema 2 常量、Schema 1→2 Migration Registry、Frozen Save/Golden Hash、View Pool、引用计数纹理缓存、macOS 四项性能基线、64 棋子纹理共享基线、1000 棋子投影与稳态分配基线、真实 macOS 2560×1440 Player 窗口采样、macOS 原生选择器选择/取消接缝、Workbench 空闲 UI 列表失效缓存、Unity 连接诊断脚本、棋子库虚拟化缩略图网格、UX4 筛选与分区、UX5 主控台分区、Visual Scripting 清理、生产 pacing 采样入口；最新真实窗口渲染承载 p95 `4.5495 ms`、分配 p95 `0 B` 通过，生产限速 pacing 长时 p95 `17.2119 ms` 未通过；10 分钟操作 Soak 通过，60 分钟操作 Soak 本轮运行约 50 分钟后未生成结果；macOS universal BuildResult Success，双架构已确认 | UX6–UX7、正式场景纹理/长时间性能门槛、严格生产 60 FPS pacing、Windows/跨平台/强制退出矩阵；当前工程直接构建仍有遗留 Library/Bee TypeDB 诊断；Unity MCP bridge 未安装；Unity Cloud symbols 上传 token 未配置 |

## 当前执行顺序

1. 已完成 Journal 状态缓存、损坏尾部续写、HEAD 缺失/损坏扫描、generation 冲突和旧 HEAD 保留自动化。
2. 已完成地图运行时索引、版本化 `WorldChangeSet`、CommandEnvelope、AcceptedOperationBatch 和 Delta Undo/Redo。
3. 已接入 Journal 命令 Payload 重放，并保持旧开发存档读取路径。
4. 已建立单写入异步保存队列和明确的保存状态；运行时手动保存和事务阈值自动保存均进入队列。
5. 已为 `M2ProjectStore` 增加 OS 级 `.save.lock`、锁超时和 expected generation 的锁内复查；故障注入可验证 revision 写入失败、权限失败时 staging 清理和旧 HEAD 保留。
6. 已用正交 Camera、Tilemap 与 UI Toolkit 建立正式 Workbench，并补齐 Input System 编辑/视口、选择、剪贴板、旋转、图层排序及门/箱对象；左右工具/Inspector 栏已改为可滚动，低高度窗口不会裁掉图片选择等入口。
7. 已完成 M4 棋子库纯数据、四种位置关系、关系循环校验、资产去重、版本化命令/Journal 接入、占位/真实纹理投影、路径导入、macOS 原生选择器选择/取消路径、64 棋子纹理共享基线、堆叠/收纳/附着操作 UI；正式图片管理视觉体验、正式场景纹理/长时间性能仍待补齐。
8. 已完成 M5 多地图槽位、迷雾、动态标注和交互状态的权威命令切片；已完成 M6A 规则决策证明、M6B Loopback Snapshot/Delta Tail/Audience Projection；已完成 M7 Save v1/schema 2 冻结、迁移注册表、Frozen Save、Golden Hash、对象池、纹理缓存和性能采样入口。
9. 已补齐 M5 Hierarchy、地图右键通用菜单、门箱对象增删/开关/旋转和主持预览入口；新增 78/78 EditMode、6/6 PlayMode 证据，并记录 macOS 四项性能基线、64 棋子纹理共享基线及 Workbench 空闲 UI 列表缓存回归。
10. 已补齐 M5 迷雾笔刷批处理、动态标注拖动和 Audience Projection 玩家预览只读路径；新增完整证据为 84/84 EditMode、12/12 PlayMode。此前测试日志中的 Licensing Module access-token 提示属于无登录 token 时的可选远程刷新，不影响本地 entitlement；当前正式入口已固定版本专用通道。
11. 2026-08-28 完成 macOS High 档与帧率策略调整；真实 Player 以 2560×1440、1000 棋子运行，解除采样限速后的渲染承载 p95 `4.5495 ms`、托管分配 p95 `0 B`，并刷新固定通道下的 EditMode `85/85`、PlayMode `13/13` 证据。生产 60 FPS 限速帧 pacing 和长时间 Soak 仍待验证。
12. 2026-08-28—29 增加生产 60 FPS pacing 采样入口和 macOS 长时脚本；新构建 Smoke 通过，脚本默认通过 LaunchServices `open -n -W` 启动 Player，短时真实窗口采样分配 p95 `0 B`、帧时间 p95 `16.7637 ms`，严格 pacing 门槛未通过；216000 帧长时采样运行约 60 分钟、退出码 `0`、分配 p95 `0 B`、帧时间 p95 `17.2119 ms`，长时稳定性通过但严格 pacing 未通过；直接执行路径连续触发 macOS AppKit/LaunchServices `SIGABRT`，已保留为诊断回退，超时清理现改为只终止本次新建 Player。许可证启动脚本新增版本专用孤儿客户端清理，并通过握手探针和完整测试。
13. 2026-08-29—30 修复 SaveQueue 任务链、Canonical Hash 空对象兼容、Tilemap 重复组件和有限历史缓存；最新 EditMode `87/87`、PlayMode `13/13`，重新构建 macOS Universal IL2CPP 并通过产物校验；10 分钟操作 Soak 通过（69/69 次保存、重开 Hash 一致）。60 分钟操作 Soak 本轮约运行 50 分钟后未生成结果文件，保持未验证，不再重复运行；已增加 Windows x64 IL2CPP 构建入口和 Windows 验证交接文档。
14. 2026-08-30 完成 M4 Workbench 棋子交互：单选/多选、框选、拖动 Ghost、批量移动/旋转/翻面/显隐/删除，以及显式发布建棋盘入口。批量操作均通过 Command Bus、Undo/Redo、Envelope、Journal 与异步保存链路；完整回归为 EditMode `92/92`、PlayMode `14/14`。空稳定 ID 的棋盘会被明确修复，避免后续放置棋子得到不可操作的领域错误。
15. 2026-08-30 完成 M5 多地图工作区闭环：从地图列表和输入框走同一安全切图路径，切图前保存当前草稿，切图后恢复各自相机、图层、工具、工作区和素材选择，并清理临时棋子交互。工作区状态改为每张地图独立文件，旧单文件保持读取兼容；完整回归为 EditMode `93/93`、PlayMode `15/15`。
16. 2026-08-31 在 M5 修改后重建 macOS Universal IL2CPP 并通过 Player Smoke：产物为 `x86_64 + arm64`，IL2CPP 元数据完整，未检测到 Smoke 运行时失败。原工程旧 `Library/Bee` 输出 `34819` 条 NUnit 同源 TypeDB 重复登记诊断；新增临时工作树构建脚本已实测干净构建 `errors=0`、TypeDB `0`、C# `0`，且自动删除临时生成物，不触碰原有 7 GB Library。详见 `Docs/Evidence/M7-macos-build-20260831.md`。

17. 已接受 Workbench UX 重构为 macOS 收尾主线：项目中心保留弹窗，地图制作、棋子库和主控台改为三个完整工作区；地图制作必须提供持续可见的有效区域外框、暗化不可编辑的地图外区域、越界 Ghost 反馈和“适应地图”；右侧改为棋子/对象/选区情境 Inspector。按 UX1–UX7 批次执行，详见 [Workbench UX 重构计划](./Workbench-UX重构计划.md)。

18. 2026-08-31 完成 UX5 主控台第一批：在既有主控台工作区内将主持地图管理、主持状态、迷雾、标注和交互对象拆成带标题/提示的独立区块，并为工作区隔离补充 PlayMode 断言；移除 Unity 6 不支持的 USS `line-height`。固定通道全量回归为 EditMode `93/93`、PlayMode `16/16`，许可证握手通过；证据见 `Docs/Evidence/M7-workbench-ux5-20260831.md`。本轮不重复 60 分钟 Soak。

Windows IL2CPP、Windows 原子写盘、跨平台互开和双平台强制退出矩阵是发布阻塞项，最迟必须在 M7 Beta 候选前通过。


## 2026-08-31 UX1 执行汇报

- 完成三个顶层工作区：地图制作、棋子库、主控台；顶部按钮切换整个页面容器，旧 Workbench Tab 仅保留为兼容节点。
- 地图制作增加持续可见的闭合边界线；地图外坐标继续拒绝编辑，切到棋子库或主控台后地图输入被禁用。
- 棋子库新增独立右侧状态面板，展示定义/实例、位置、旋转、翻面、显隐、堆叠、容器和附着状态。
- 新增 PlayMode 回归：三工作区互斥显示、边界存在与切换保护。
- 证据：TestResults_EditMode_20260831_023656.xml 93/93；TestResults_PlayMode_20260831_023912.xml 16/16；两次许可证握手均通过。
- 本轮未运行 60 分钟 Soak，遵循此前已确认的跳过决定。

- UX2 第一批已完成：地图工具移入顶部工具栏并有明确选中态；图层显隐/锁定改为图标和 Tooltip；地图有效区增加暖色底板与运行时网格；素材按钮改为带色块缩略图的卡片；拖动棋子 Ghost 越界时显示警告色，释放仍整体拒绝。
- UX2 第一批对应验证为 EditMode 93/93、PlayMode 16/16；本轮不重复运行 60 分钟 Soak。
- UX3 已完成：右侧 Inspector 根据当前情境显示地图摘要、格子选区、门/箱子对象、单棋子状态或多棋子批量状态；Revision、数量和稳定 ID 收进折叠诊断区；对象与棋子选择互斥；棋子操作按钮按选择状态启用/禁用；选中棋子增加独立高亮轮廓。
- UX3 对应验证为 EditMode 93/93、PlayMode 16/16；Inspector 情境切换、操作启用状态和棋子高亮断言通过，许可证通道握手通过；本轮不重复运行 60 分钟 Soak。
- 下一步进入 UX4 棋子库页面，补齐筛选维度、定义详情和导入流程的视觉分区。

## 2026-08-31 UX7 第一批执行汇报

- 完成共享 `WorkbenchStyles.uss` 的视觉兼容与布局收尾，保留原 GUID，避免 Unity 资源引用断裂。
- 恢复项目中心、旧诊断 Tab、棋子库网格、地图工具栏、图层状态、地图边界反馈和 Inspector 所需选择器；统一深色外壳、暖色地图画布及按钮/素材/棋子卡片状态。
- 固定许可证通道回归：EditMode `97/97`、PlayMode `16/16`，0 失败、0 跳过；未发现编译错误、USS 无效属性或测试失败。
- 证据见 `Docs/Evidence/M7-workbench-ux7-20260831.md`。本轮不重复运行 60 分钟 Soak；UX7 真实 Player 视觉复验、严格 pacing 和 Windows 矩阵仍未关闭。

## 2026-08-31 UX7 第二批执行汇报

- 顶部三个工作区按钮现在有稳定 Tab 顺序 1/2/3 和可见焦点；文本框获得焦点时，全局 Cmd/Ctrl 快捷键让位给文本编辑，Escape 仍可全局取消操作。
- 新增 PlayMode 焦点识别、失焦和工作区按钮导航断言。第一次编译发现并修复 `Focusable` 类型访问错误后，固定许可证通道复跑 EditMode `97/97`、PlayMode `16/16`，0 失败、0 跳过。
- 证据已更新至 `Docs/Evidence/M7-workbench-ux7-20260831.md`；真实 Player 键盘手感、中文布局、窗口缩放和视觉截图仍未关闭。
