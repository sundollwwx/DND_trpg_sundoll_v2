# 项目文档

此目录存放跨 Unity 工程的产品规划与对外汇报。

- `Planning/`：完整开发计划与简明进度汇报。
- `../M0-Spike/`：早期一次性技术验证，仅作历史参考。
- `../SundollWorld/Docs/`：正式工程的阶段报告、操作指南与验证证据。

日常使用请返回根目录，双击 `01-启动SundollWorld.command`；只有继续开发时才使用 `02-在Unity中编辑SundollWorld.command`。
